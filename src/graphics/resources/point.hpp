
#pragma once
#include <glm/glm.hpp>
#include <string>

namespace chionia {

    struct Point {

        std::string id;
        glm::vec3 position;
        //glm::vec3 color;  // next update

    };
}